name: FTP Deployment

on:
  push:
    branches:
      - main  # WICHTIG: Prüfe, ob dein Branch wirklich 'main' heißt (sonst 'master')

jobs:
  web-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Code auschecken
        uses: actions/checkout@v4

      - name: 📂 Sync files via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ftp.gokertech.at
          username: gokertech
          password: Phah2eeD
          server-dir: ftp://ftp.gokertech.at/public_html/
          protocol: ftp
          port: 21
